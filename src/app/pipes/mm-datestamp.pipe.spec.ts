import { MmDatestampPipe } from './mm-datestamp.pipe';

describe('MmDatestampPipe', () => {
  it('create an instance', () => {
    const pipe = new MmDatestampPipe();
    expect(pipe).toBeTruthy();
  });
});
